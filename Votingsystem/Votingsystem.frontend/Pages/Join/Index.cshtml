@page
@model VotingSystem.Frontend.Pages.Join.IndexModel
@{
    ViewData["Title"] = "Join a Poll";
}

@*
    JOIN PAGE (Placeholder)
    -----------------------
    Next step we will build:
    - session ID input
    - QR placeholder
    - join button -> goes to poll
*@

<h1 class="text-2xl font-semibold">Join a Poll (placeholder)</h1>
<p class="text-slate-400 mt-2">Next step we build the Join flow (session ID + QR).</p>